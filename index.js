import React from "react";
import ReactDOM from "react-dom";
import App from "./src/App.jsx";

// set the react-dom and render the App componenet with the Provider
ReactDOM.render(<App />, document.getElementById("app"));1